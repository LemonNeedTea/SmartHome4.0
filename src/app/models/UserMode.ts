export default class UserMode {
       public id: number;
       public img: string;
       public name: string;
       public selected: boolean;
       public sort: number;
       public userId: number;
}